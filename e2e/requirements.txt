# Python dependencies for the Floci Studio E2E harness (see e2e/README.md).
#
# Installed into a dedicated e2e/.venv so a `uv sync --project mcp` (which the
# web SessionStart hook runs) never prunes them.
#
#   uv venv e2e/.venv
#   uv pip install --python e2e/.venv/bin/python -r e2e/requirements.txt
#
# moto[server] : in-memory AWS-compatible API used as a stand-in for the Floci
#                engine container when its image can't be pulled (CI / sandbox).
# boto3        : used by seed.py to populate realistic resources.
# awscli       : the sidecar shells out to `aws`; keep it on PATH for the harness.
moto[server]>=5.0
boto3>=1.34
awscli>=1.32
