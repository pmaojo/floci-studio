{
  "id": "mailpit",
  "name": "Mailpit (SMTP)",
  "description": "Email and SMTP testing tool for developers with a web interface.",
  "version": "1.0.0",
  "accessUrl": "http://localhost:{{MAILPIT_WEB_PORT}}",
  "variables": [
    {
      "key": "MAILPIT_SMTP_PORT",
      "label": "SMTP Port",
      "type": "number",
      "default": 1025,
      "description": "Port on your host machine to bind the SMTP server."
    },
    {
      "key": "MAILPIT_WEB_PORT",
      "label": "Web Interface Port",
      "type": "number",
      "default": 8025,
      "description": "Port on your host machine to bind the Mailpit web interface."
    }
  ]
}