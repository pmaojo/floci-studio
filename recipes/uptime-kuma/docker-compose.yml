services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    ports:
      - "${UPTIME_KUMA_PORT}:3001"
    volumes:
      - uptime-kuma-data:/app/data
    restart: unless-stopped

volumes:
  uptime-kuma-data:
