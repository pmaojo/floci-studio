services:
  ollama:
    image: ollama/ollama:latest
    container_name: floci-ollama
    ports:
      - "${OLLAMA_PORT}:11434"
    environment:
      OLLAMA_KEEP_ALIVE: "${OLLAMA_KEEP_ALIVE}"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped

volumes:
  ollama_data:
