{
  "id": "vault",
  "name": "HashiCorp Vault (Dev Mode)",
  "description": "Secrets management and encryption-as-a-service in a ready-to-use dev server. Store API keys, database credentials and certificates, then read them from your apps over HTTP. A local stand-in for AWS Secrets Manager and Parameter Store.",
  "version": "1.18.0",
  "accessUrl": "http://localhost:{{VAULT_PORT}}",
  "variables": [
    {
      "key": "VAULT_PORT",
      "label": "HTTP / UI Port",
      "type": "number",
      "default": 8200,
      "description": "Host port for the Vault HTTP API and web UI."
    },
    {
      "key": "VAULT_DEV_ROOT_TOKEN",
      "label": "Root Token",
      "type": "password",
      "default": "root",
      "description": "Root token used to authenticate against the dev server."
    }
  ]
}
