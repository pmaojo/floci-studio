services:
  nats:
    image: nats:2.10-alpine
    ports:
      - "${NATS_PORT}:4222"
      - "${NATS_MONITORING_PORT}:8222"
    command: ["-js", "-m", "8222", "--name", "floci-nats"]
    restart: unless-stopped
